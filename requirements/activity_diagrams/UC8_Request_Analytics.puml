@startuml UC8_Request_Analytics_Activity_Diagram

title UC8: Request Analytics - Activity Diagram

start
:User selects "Request Analytics" option;
repeat
  :System displays analytics request form;
  :User specifies parameters
  (cities, date range, services, metrics);
  :User submits request;
  :System validates parameters;

  if (Parameters valid?) then (yes)
    if (Aggregates needed?) then (yes)
      :System invokes UC14 (Calculate Aggregates);
    endif

    if (Report generation times out?) then (yes)
      :System queues request for async processing;
      note right: Exception E1 - Report generation timeout
      stop
    else (no)
      :System generates analytics report;
      :System presents report preview;
      stop
    endif
  else (no)
    :System displays error;
  endif
repeat while (Retry?) is (yes) not (no)

@enduml
